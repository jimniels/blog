{
  // Data I generated before hand i'd pass here
  (data) = {
    links: [
      { domain: "twitter.com", name: "Twitter" },
      { domain: "github.com", name: "GitHub" },
      { domain: "linkedin.com", name: "LinkedIn" }
    ]
  }
  // Look at everything in `site` and turn each `.jse` file into either
  // 1. a single fail with .jse removed (index.json.jse -> index.json)
  // 2. multiple files ($link.jse -> [github.json, linkedin.json, twitter.json])
  ...Tree.map(./site, {
    deep: true
    extension: ".jse->"
    value: (file) => file(data)
  })
}